{
  "name": "sem5",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build && mv dist/index.html dist/project.html && cp -r dist/ ../server/ && cp -r ../server/.cache/* ../server/dist/",
    "serve": "vite preview"
  },
  "dependencies": {
    "axios": "^0.23.0",
    "phaser": "^3.55.2",
    "socket.io-client": "^4.1.3"
  },
  "devDependencies": {
    "vite": "^2.6.10"
  }
}
